# Normalize text files to LF in the repo, auto-convert on checkout
* text=auto eol=lf

# Treat these as binary — never apply line-ending conversion (prevents corruption)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
